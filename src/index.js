import React from 'react';
import ReactDOM from 'react-dom';
import 'typeface-roboto';
import RootTemplate from './Templates/RootTemplate';

ReactDOM.render(<RootTemplate />, document.getElementById('root'));

export default null;
