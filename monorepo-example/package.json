{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "bootstrap:hoist": "lerna bootstrap --hoist",
    "start:api": "lerna run start --scope=api --stream",
    "start:frontend": "lerna run start --scope=frontend --stream"
  }
}
